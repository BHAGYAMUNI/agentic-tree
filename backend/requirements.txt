annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
bcrypt==4.0.1
click==8.3.1
colorama==0.4.6
ecdsa==0.19.1
fastapi==0.131.0
greenlet==3.3.2
h11==0.16.0
idna==3.11
passlib==1.7.4
psycopg2-binary==2.9.11
pyasn1==0.6.2
pydantic==2.12.5
pydantic_core==2.41.5
python-jose==3.5.0
python-multipart==0.0.22
rsa==4.9.1
six==1.17.0
SQLAlchemy==2.0.46
starlette==0.52.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.41.0
# Optional LLM dependencies (install if you plan to enable USE_LLM_AGENT)
# LangChain is used by the LangGraph agent.  The package went through a
# breaking restructure in the 1.x series that moved `schema` and other
# classes around; pin to anything below 1.0 so users don't accidentally pull
# in a version we haven't tested against.  If you intentionally upgrade to a
# later major release you'll also need to update the imports in
# `langgraph_agent.py` accordingly.
langchain>=0.0.210,<1.0
langchain-openai>=0.0.1
langgraph>=0.0.20
openai>=1.0.0
pytest